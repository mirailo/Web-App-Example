@page
@model MyFirstRazorWebPage.Pages.Users.UploadPictureModel
@{
    ViewData["Title"] = "Upload User Picture";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Admin Page - Upload User Picture</title>
</head>


<body>
    <p>
        <h1 style="text-align: center;">Admin Page - Upload User Picture</h1>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Users[0].FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Users[0].EmailAdd)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Users)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmailAdd)
                    </td>
                    <td>
                        <a asp-page="/BrowsePicture/Index" asp-route-id="@item.EmailAdd">Upload Picture</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    </body>
</html>
