@page
@model MyFirstRazorWebPage.Pages.StaffPage.EditCustomerModel
@{
    ViewData["Title"] = "Staff Page - Update Customer Details";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>

</head>
<body>

    <h1 style="text-align:center">Staff Page : Update Customer Details</h1>
    <hr />

    <div>
        <img id="displayImg" src="~/Images/@Model.pathPicture" alt="Image not found" style=" max-width:180px;" /><br>
        <p>
            File Name : @{

                if (Model.pathPicture != "DefaultPic.jpeg")
                {
                    @Model.pathPicture
                }
                else
                {
                    var text = "The user does not have a set recommended hair product yet. Please upload a picture of the recommended hair product for this customer.";
                    Console.WriteLine(text);
                    @: @text

                    Model.pathPicture = "DefaultPic.jpeg";
                    Console.WriteLine("After defaultted : " + Model.pathPicture);
                }


            }
        </p>
    </div>

    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Customer.ID" />
                <div class="form-group">
                    <label asp-for="Customer.FirstName" class="control-label"></label>
                    <input asp-for="Customer.FirstName" class="form-control" />
                    <span asp-validation-for="Customer.FirstName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Customer.EmailAdd" class="control-label"></label>
                    <input asp-for="Customer.EmailAdd" class="form-control" />
                    <span asp-validation-for="Customer.EmailAdd" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Customer.Password" class="control-label"></label>
                    <input asp-for="Customer.Password" class="form-control" />
                    <span asp-validation-for="Customer.Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-page="./CustomerDetails">Back to List</a>|

    </div>

    <div>
        @{

            if (Model.pathPicture == "DefaultPic.jpeg")
            {

                <a asp-page="/BrowsePicture/Index" asp-route-id="@Model.Customer.ID">Upload Picture of Hair Product</a>
            }
            else
            {
                <a asp-page="/StaffPage/DeletePicture" asp-route-id="@Model.Customer.ID">Delete Picture of Hair Product</a>
            }


        }

    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }


    <script>
        function readURL(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    document.getElementById("displayImg").setAttribute("src", e.target.result);
                };
                reader.readAsDataURL(input.files[0]);
            }
        }
        function delFunction() {
            var txt;
            if (confirm("Are you sure want to delete the current hair product picture?")) {
                var value = "@Model.pathPicture";
                txt = "Picture deleted";
                location.replace("StaffPage/ManagePicture") + value;
            } else {
                txt = "Picture not deleted";
            }
        }



    </script>

    <script src="~/js/site.js" asp-append-version="true"></script>

</body>
</html>

